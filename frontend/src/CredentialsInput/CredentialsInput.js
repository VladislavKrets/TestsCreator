function CredentialsInput(props){
    return <form className={'credentials-container'}>
            <input type={'text'}/>
            <input type={'password'}/>
            <input type={'submit'}/>
        </form>
}

export default CredentialsInput;